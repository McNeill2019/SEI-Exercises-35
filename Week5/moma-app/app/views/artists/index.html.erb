<h1>All Artists</h1>

<ul>
  <% @artists.each do |artist| %>
  <li>

    <%= link_to artist.name, artist_path(artist.id) %> (<%= artist.nationality %>)

    <!-- <a href="<%= artist_path(artist.id) %>">
      <%= artist.name %></a>(<%= artist.nationality %>)
    -->

  </li>
  <% end %>
</ul>
